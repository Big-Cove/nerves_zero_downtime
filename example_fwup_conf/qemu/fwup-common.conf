#
# Firmware metadata
#

# All of these can be overriden using environment variables of the same name.
#
#  Run 'fwup -m' to query values in a .fw file.
#  Use 'fw_printenv' to query values on the target.
#
define(NERVES_FW_PRODUCT, "Nerves Firmware")
define(NERVES_FW_DESCRIPTION, "")
define(NERVES_FW_VERSION, "${NERVES_SDK_VERSION}")
define(NERVES_FW_PLATFORM, "qemu_aarch64")
define(NERVES_FW_ARCHITECTURE, "arm")
define(NERVES_FW_AUTHOR, "The Nerves Team")

define(NERVES_FW_DEVPATH, "/dev/vda")
define(NERVES_FW_APPLICATION_PART0_DEVPATH, "/dev/vda6") # Application is logical partition 6
define(NERVES_FW_APPLICATION_PART0_FSTYPE, "f2fs")
define(NERVES_FW_APPLICATION_PART0_TARGET, "/root")
define(NERVES_PROVISIONING, "${NERVES_SYSTEM}/images/fwup_include/provisioning.conf")

# Default paths if not specified via the commandline
define(ROOTFS, "${NERVES_SYSTEM}/images/rootfs.squashfs")

# This configuration file will create an image that has an MBR with 3 primary
# partitions and 1 extended partition containing 2 logical partitions:
#
# +----------------------------+
# | MBR                        |
# +----------------------------+
# | U-Boot environment         |
# +----------------------------+
# | Kernel A                   |
# +----------------------------+
# | Kernel B                   |
# +----------------------------+
# | Kernel C                   |
# +----------------------------+
# | p0: Rootfs A (squashfs)    |
# +----------------------------+
# | p1: Application (f2fs)     |
# +----------------------------+
# | p2: Extended partition     |
# |   p5: Rootfs B (squashfs)  |
# |   p6: Rootfs C (squashfs)  |
# +----------------------------+

# The U-Boot environment is written directly to eMMC. It is not in any partition.
define(UBOOT_ENV_OFFSET, 16)
define(UBOOT_ENV_COUNT, 256)   # 128 KB
define(KERNEL_COUNT, 102400) # 50 MB
define-eval(KERNEL_A_OFFSET, "${UBOOT_ENV_OFFSET} + ${UBOOT_ENV_COUNT}")
define-eval(KERNEL_B_OFFSET, "${KERNEL_A_OFFSET} + ${KERNEL_COUNT}")
define-eval(KERNEL_C_OFFSET, "${KERNEL_B_OFFSET} + ${KERNEL_COUNT}")

# Primary partition 0: Rootfs A
define-eval(ROOTFS_A_PART_OFFSET, "${KERNEL_C_OFFSET} + ${KERNEL_COUNT}")
define(ROOTFS_A_PART_COUNT, 102400) # 50 MB

# Primary partition 1: Rootfs B
define-eval(ROOTFS_B_PART_OFFSET, "${ROOTFS_A_PART_OFFSET} + ${ROOTFS_A_PART_COUNT}")
define(ROOTFS_B_PART_COUNT, 102400) # 50 MB

# Primary partition 3: Extended partition container (MUST be partition 3 per MBR spec)
# Contains logical partitions for Rootfs C and Application data
define-eval(EXTENDED_PART_OFFSET, "${ROOTFS_B_PART_OFFSET} + ${ROOTFS_B_PART_COUNT}")
define(ROOTFS_C_PART_COUNT, 102400) # 50 MB
define(APP_PART_COUNT, 307200) # 150 MB
define-eval(EXTENDED_PART_COUNT, "${ROOTFS_C_PART_COUNT} + ${APP_PART_COUNT} + 2048") # Extra for EBR headers

# Logical partition 5: Rootfs C (first logical partition in extended)
define-eval(ROOTFS_C_PART_OFFSET, "${EXTENDED_PART_OFFSET} + 1024") # Offset for EBR

# Logical partition 6: Application data (second logical partition in extended)
define-eval(APP_PART_OFFSET, "${ROOTFS_C_PART_OFFSET} + ${ROOTFS_C_PART_COUNT} + 1024") # Offset for second EBR

define-eval(APP_PART_REAR_PAD, "${APP_PART_OFFSET} + ${APP_PART_COUNT} - 256")

# Firmware archive metadata
meta-product = ${NERVES_FW_PRODUCT}
meta-description = ${NERVES_FW_DESCRIPTION}
meta-version = ${NERVES_FW_VERSION}
meta-platform = ${NERVES_FW_PLATFORM}
meta-architecture = ${NERVES_FW_ARCHITECTURE}
meta-author = ${NERVES_FW_AUTHOR}
meta-vcs-identifier = ${NERVES_FW_VCS_IDENTIFIER}
meta-misc = ${NERVES_FW_MISC}

